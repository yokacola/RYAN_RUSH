// =========================================================================
//  [리안러쉬] 게임 수치 조절 설정 파일 (GAME CONFIG)
// =========================================================================

export const GAME_CONFIG = {
  // -----------------------------------------------------------------------
  // 1. 캐릭터 (리안) 설정
  // -----------------------------------------------------------------------
  player: {
    width: 165,             // 캐릭터 가로 크기
    height: 330,            // 캐릭터 세로 크기
    groundY: 575,           // 도로 위 발 접지 높이
    cropInsetX: 10,         // 스프라이트 좌우 테두리 여백
    cropInsetY: 14,         // 스프라이트 상하 테두리 여백
    gravity: 1450,
    jumpForce: -690,
    doubleJumpForce: -640,
    surpriseDuration: 0.95,
    runFrameSequence: [0, 1, 3, 2, 4, 5, 7], // 달리기 애니메이션 시트 순서 번호 배열 (0~7 원하는 순서대로 배치 가능)
    runFrameInterval: 0.085,                    // 프레임 재생 간격(초, 작을수록 빠르게 달림)
    frameGroundOffsets: [0, 0, 0, 0, 0.174, 0.174, 0.174, 0.174],
    hitScaleRatio: 0.8,
    surpriseStarOffsetX: -20, // 부딪혔을 때 머리 위 별 X 오프셋 (왼쪽 20px 이동)
  },

  // -----------------------------------------------------------------------
  // 2. 아이템 (사탕, 쿠키, 빵) 설정 (2배 확대: 160x160)
  // -----------------------------------------------------------------------
  items: {
    width: 160,             // 가로 크기
    height: 130,            // 세로 크기
    lowY: 425,              // 바닥 쪽 높이
    highY: 245,             // 공중 쪽 높이
    spawnRatio: 0.75,       // 등장 확률
    bounceSpeed: 4.5,       // 상하 바운스 속도
    bounceHeight: 16,       // 바운스 높이 (px)
    minClusterCount: 1,     // 줄지어 나오는 최소 개수 (1개)
    maxClusterCount: 5,     // 줄지어 나오는 최대 개수 (5개)
    clusterSpacing: 145,    // 줄지어 나올 때 아이템 간격 (px)
  },

  // -----------------------------------------------------------------------
  // 3. 악당 장애물 설정 (원본 비율 보존 & 그림자 완벽 밀착)
  // -----------------------------------------------------------------------
  obstacles: {
    baseHeight: 180,        // 기준 세로 높이 (가로는 원본 종횡비에 따라 자동 계산되어 찌그러짐 0%)
    groundY: 575,           // 도로 바닥 접지선
    imageYOffset: 8,        // 몬스터 이미지를 아래로 내려 그림자와 완벽 밀착 (px)
    shadowYOffset: 4,       // 그림자 Y 오프셋 (px)
    bounceBaseSpeed: 4.5,
    goldenRatio: 1.61803398875,
    bounceHeight: 9,        // 아주 살짝 위로 통 토동 뛰는 높이 (과도하게 뜨지 않도록 조절)
  },

  // -----------------------------------------------------------------------
  // 4. 스폰 간격 (사물/그룹 간 안전 거리 - 절대 겹침 방지)
  // -----------------------------------------------------------------------
  spawner: {
    minGap: 420,            // 사물(그룹) 간 최소 안전 간격 (px)
    maxGap: 680,            // 사물(그룹) 간 최대 안전 간격 (px)
  },

  // -----------------------------------------------------------------------
  // 5. 게임 주행 속도 및 배경 레이어 속도 비율
  // -----------------------------------------------------------------------
  speed: {
    run: 380,
    title: 150,
    surprise: 180,
    layer1Sky: 0.20,
    layer2Mountain: 0.60,
    layer3City: 1.50,
    layer4Road: 2.00,
  },

  // -----------------------------------------------------------------------
  // 6. 사운드 및 배경음악 설정
  // -----------------------------------------------------------------------
  audio: {
    bgmVolume: 0.45,        // 배경음악 볼륨 (0.0 ~ 1.0)
  },

  // -----------------------------------------------------------------------
  // 7. UI 설정 (타이틀 로고 및 시작 버튼 위치)
  // -----------------------------------------------------------------------
  ui: {
    titleY: 160,            // 타이틀 로고 기본 Y 높이
    titleWidth: 520,        // 타이틀 로고 가로 너비
    btnStartY: 630,         // 시작 버튼 Y 높이
  },

  // -----------------------------------------------------------------------
  // 8. 배경 레이어 설정 (각 레이어 Y 위치, 높이, 좌우 클리핑 마진, 스크롤 속도)
  // -----------------------------------------------------------------------
  background: {
    // 1. 하늘 배경 (assets/background/bg_city.png)
    layer1Sky: {
      y: 0,                   // 렌더링 Y 위치 (px)
      height: 720,            // 렌더링 세로 높이 (px)
      cropLeft: 2,           // 좌측 클리핑 마진 (px)
      cropRight: 2,          // 우측 클리핑 마진 (px)
      seamOverlap: 2,         // 타일 경계선 겹침 폭(px)
      scrollSpeed: 0.10,      // 스크롤 속도 배율
    },
    // 2. 먼 산 (assets/background/layer_1_sky.png)
    layer2Mountain: {
      y: 45,                 // 렌더링 Y 위치 (px)
      height: 380,            // 렌더링 세로 높이 (px)
      cropLeft: 0,           // 좌측 클리핑 마진 (px)
      cropRight: 0,          // 우측 클리핑 마진 (px)
      seamOverlap: 2,         // 타일 경계선 겹침 폭(px)
      scrollSpeed: 0.45,       // 스크롤 속도 배율
    },
    // 3. 마을 & 건물들 (assets/background/layer_2_city.png)
    layer3City: {
      y: 100,                 // 렌더링 Y 위치 (px)
      height: 450,            // 렌더링 세로 높이 (px)
      cropLeft: 5,           // 좌측 클리핑 마진 (px)
      cropRight: 5,          // 우측 클리핑 마진 (px)
      seamOverlap: 2,         // 타일 경계선 겹침 폭(px)
      scrollSpeed: 0.95,     // 스크롤 속도 배율
    },
    // 4. 아스팔트 도로 (assets/background/layer_3_road.png)
    layer4Road: {
      y: 530,                 // 렌더링 Y 위치 (px)
      height: 190,            // 렌더링 세로 높이 (px)
      cropLeft: 5,           // 좌측 클리핑 마진 (px)
      cropRight: 5,          // 우측 클리핑 마진 (px)
      seamOverlap: 2,         // 타일 경계선 겹침 폭(px)
      scrollSpeed: 1.5,      // 스크롤 속도 배율
    }
  }
};